# 生产环境 Supabase 存储配置
# 用于本地开发环境强制使用云端存储，确保与生产环境一致

# Supabase 项目配置 - PMA 项目
SUPABASE_URL=https://iqcyimnjtnmomvfuwjzw.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImlxY3lpbW5qdG5tb212ZnV3anp3Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc1NDc1Mjg5NiwiZXhwIjoyMDcwMzI4ODk2fQ.ivIwtz0Icp0eBibB4RAVh9iAULTHhCkKfPF9QOFnvfY

# 系统标识（自动基于APP_DOMAIN识别）
# SYSTEM_IDENTIFIER=PMA-SA  # 已改为自动识别，无需手动设置

# 存储桶配置（多存储桶支持）
SUPABASE_BUCKET_INVOICE=invoice-images
SUPABASE_BUCKET_PRODUCT=product-images  
SUPABASE_BUCKET_RD_PRODUCT=rd-product-images

# 强制启用云端上传（确保本地使用云端存储）
FORCE_CLOUD_UPLOAD=true

# 使用说明：
# 1. 此配置文件用于本地开发环境强制使用生产环境Supabase存储
# 2. 确保本地测试和云端生产环境行为完全一致
# 3. 使用 run_with_supabase_prod.sh 脚本启动应用
# 4. 或手动加载：export $(cat .env.supabase.prod | grep -v '^#' | xargs)

# 存储桶权限要求：
# - 所有存储桶需要配置正确的RLS策略
# - 允许匿名用户上传、读取、更新文件
# - 确保生产环境和开发环境权限一致

# 重要提醒：
# - 此配置用于开发测试，包含生产环境凭据
# - 请确保 .gitignore 中排除此文件避免泄露
# - 定期更新密钥以保证安全性