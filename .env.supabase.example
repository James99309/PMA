# Supabase 配置用于本地测试云端上传功能
# 复制此文件为 .env.supabase 并填入您的实际 Supabase 配置

# Supabase 项目配置
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-anon-public-key-here
SUPABASE_BUCKET=product-images

# 强制启用云端上传（用于本地测试）
FORCE_CLOUD_UPLOAD=true

# 使用方法：
# 1. 复制此文件为 .env.supabase
# 2. 填入您的 Supabase 项目信息
# 3. 在本地运行前执行：source .env.supabase 或 export $(cat .env.supabase | xargs)
# 4. 启动应用：python run.py
# 5. 测试报销单文件上传功能

# 注意事项：
# - 确保 SUPABASE_BUCKET 存在且有正确的权限策略
# - SUPABASE_KEY 应该是 anon public key，不是 service_role key
# - 测试完成后可以删除这些环境变量恢复本地文件上传