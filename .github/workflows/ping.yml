name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # 每 10 分钟运行一次，UTC时间
    workflow_dispatch: # 允许手动触发
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Ping to Render
        run: curl -s https://pma-ipwv.onrender.com > /dev/null
